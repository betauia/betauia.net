---
import BetaLANLayout from "./BetaLAN-Layout.astro";
const { UseGamerFont = true } = Astro.props;
---

<BetaLANLayout UseGamerFont={UseGamerFont}>
    <div class="bg-slate-800 bg-opacity-90 py-3 px-2 md:px-4 rounded-lg mt-4 max-w-4xl mx-auto text-yellow-100 tournament-content">
        <slot />
    </div>
</BetaLANLayout>

<style>
    .tournament-content :global(.title) {
        @apply text-4xl md:text-5xl font-bold text-center mb-6 text-yellow-400;
    }

    .tournament-content :global(.section-title) {
        @apply text-2xl md:text-3xl font-bold mb-4 text-yellow-300 mt-6 text-center;
    }

    .tournament-content :global(.paragraph) {
        @apply text-base md:text-lg mb-4 leading-relaxed;
    }

    .tournament-content :global(.list) {
        @apply mb-4 space-y-2;
    }

    .tournament-content :global(.list-item) {
        @apply text-base md:text-lg;
    }

    .tournament-content :global(.responsive-img) {
        @apply mx-auto mb-6 rounded-lg shadow-lg max-w-full h-auto;
    }

    .tournament-content :global(.link-center) {
        @apply text-center list-none space-y-2;
    }

    /* Adjustments when gamer font is active */
    :global(.gamer-font) .tournament-content :global(.title) {
        font-size: 4rem;
        line-height: 1.1;
    }

    :global(.gamer-font) .tournament-content :global(.section-title) {
        font-size: 2.5rem;
        line-height: 1.2;
    }

    :global(.gamer-font) .tournament-content :global(.paragraph),
    :global(.gamer-font) .tournament-content :global(.list-item) {
        font-size: 1.5rem;
        line-height: 1.4;
    }
</style>