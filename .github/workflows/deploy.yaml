name: Deployment

on:
  push:
    branches:
      - master

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: SSH into server and deploy
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          script: |
            cd /home/mgr/services/betauia.net

            git pull origin master || { echo "Git pull failed"; exit 1; }

            docker compose -f compose.yaml -f compose.prod.yaml down || { echo "Docker compose down failed"; exit 1; }

            docker compose -f compose.yaml -f compose.prod.yaml up --build -d || { echo "Docker compose up failed"; exit 1; }
